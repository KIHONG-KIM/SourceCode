<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  </head>
  <body>
    <!-- 9/11 첫 커밋 -->
    <%- include('nav.html') %>

      <div class="container">
        <h1>할일 목록</h1>
        <ul class="list-group">
          <% for (var i = 0; i < posts.length; i++){ %>
          <li class="list-group-item">
            <h4> 할일 제목 : <%= posts[i].할일 %> </h4>
            <p> 할일 마감날짜 : <%= posts[i].날짜 %> </p>
            <a href="edit/<%= posts[i]._id %>"><button class="addHTML btn btn-primary edit" data-id="<%= posts[i]._id %>">편집</button></a>
            <button class="delete btn btn-danger delete" data-id="<%= posts[i]._id %>">삭제</button> 
          </li>
          <% } %>
        </ul>
      </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script>

      // $('.addHTML').click(function(e){

      //   var 지금누른거 = $(this);
      //   var 추가할태그 = "<div>할일<input name='title' type='text'>날짜<input name='date' type='text'><button class='edit'>확인</button><button>취소</button></div>";

      //   지금누른거.prev().after(추가할태그);
      //   console.log('추가되었음', 추가할태그);

      // });

      $('div ul').on('click', '.edit', function(e) {

        var 아이디 = $(this).data('id');
        var 할일 = $("[name=title]").val();
        var 날짜 = $("input[name=date]").val();

        $.ajax({
          method : 'put',
          url : '/edit:/${아이디}',
        }).done(function(결과){
          console.log('ajax 수정 요청 성공했어염');
        }).fail(function(결과){
        })
      });

        $('.delete').click(function(e){
                var 글번호 = e.target.dataset.id;
                var 지금누른거 = $(this);
                console.log(글번호);

          $.ajax({
                method : 'DELETE',
                url : '/delete',
                data : {_id: 글번호 }
              }).done(function(결과){
                console.log('성공했어염');
                지금누른거.closest('li').fadeOut();
              }).fail(function(xhr, textStatus, errorThrown){
                console.log(xhr, textStatus, errorThrown);
              })
          })

    </script>
  </body>
</html>